{
  "filePath": "client/src/App.tsx",
  "description": "The ClerkProvider fetch logic in App.tsx has a manual retry loop but doesn't use exponential backoff correctly. The delay is `1000 * attempt` which is linear (1s, 2s, 3s) not exponential. Use 2^attempt or a proper backoff library.",
  "successCriteria": [
    "Retry delay uses exponential backoff (e.g., 2^attempt * 1000ms)",
    "Max wait between retries is reasonable (e.g., capped at 8s)",
    "Code comment explains backoff strategy"
  ],
  "priority": "low",
  "category": "performance",
  "source": "codebase-intel",
  "createdAt": "2026-02-18T19:29:31.007Z"
}