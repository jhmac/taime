node_modules
dist
.DS_Store
server/public
uploads/
vite.config.ts.*
*.tar.gz
.apppilot/
.sneebly/
.logs/
.logs/action-log.jsonl
.logs/error-log.jsonl

# Signing keystores — NEVER commit these; loss or exposure cannot be undone
*.keystore
taime-release.keystore

# Firebase credential files — contain sensitive server keys
android/app/google-services.json
ios/App/GoogleService-Info.plist

.git.backup.*
