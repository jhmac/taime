# Sneebly Transform — Phase 1 of 5: Brand Rename

**What this does:** Renames all "AppPilot" references to "Sneebly" across the entire codebase. This is the safest change — no logic modifications, just find-and-replace with correct casing.

**Verify before starting:** Make sure you can see the existing Sneebly codebase with files like `src/elon.js`, `src/ralph-loop.js`, `src/code-engine.js`, `src/subagents/dispatcher.js`, etc.

---

## PROMPT

Search the ENTIRE codebase for all variations of "AppPilot" and replace with the correct casing of "Sneebly". This is a brand rename — no logic changes, just naming.

### Replacements to make:

| Find (case-sensitive) | Replace with | Where it appears |
|---|---|---|
| `AppPilot` | `Sneebly` | User-facing text, comments, markdown headers, dashboard HTML, error messages |
| `apppilot` | `sneebly` | Directory names, file paths, CSS classes, package name |
| `appPilot` | `sneebly` | camelCase variable names, function names |
| `app-pilot` | `sneebly` | kebab-case references, CLI commands, npm package name |
| `app_pilot` | `sneebly` | snake_case references, environment variable prefixes |
| `APP_PILOT` | `SNEEBLY` | UPPER_CASE constants, environment variables |
| `Apppilot` | `Sneebly` | Title case variants |

### Files to check (search ALL of these):

1. `package.json` — name, description, bin entries, repository
2. `README.md` — all references
3. `templates/*.md` — SOUL-template.md, AGENTS-template.md, GOALS-template.md, HEARTBEAT-template.md, IDENTITY-template.md, USER-template.md, TOOLS-template.md
4. `templates/subagents/*.md` — all subagent templates
5. `src/*.js` — all source files
6. `src/subagents/*.js` — all subagent implementations
7. `src/middleware/*.js` — middleware and dashboard
8. `src/dashboard/*.html` — dashboard HTML/JS
9. `bin/*.js` — all CLI commands
10. Any other files that contain the word "pilot" or "apppilot"

### Special attention:

- **Dashboard HTML**: The admin dashboard likely has "AppPilot" in its title, headers, and branding. Update ALL of these to "Sneebly".
- **SNEEBLY_INTERNAL_KEY**: If there's an env var called `APPPILOT_INTERNAL_KEY` or similar, rename it to `SNEEBLY_INTERNAL_KEY`. Check both the code that reads it AND the documentation that references it.
- **Directory paths**: If any code references an `apppilot/` directory, change to `sneebly/`. Check the OutputValidator and security layer.
- **Package.json bin entries**: If there are CLI commands like `apppilot` or `app-pilot`, rename to `sneebly`.
- **Comments and JSDoc**: Don't skip inline comments — they often reference the product name.

### DO NOT change:

- File content in `node_modules/`
- Git history
- Any file that doesn't actually contain an AppPilot reference

### After completing:

1. Run `grep -ri "apppilot\|app.pilot\|app_pilot\|AppPilot" --include="*.js" --include="*.md" --include="*.json" --include="*.html" .` to verify nothing was missed
2. Run `npm test` (if tests exist) to make sure nothing broke
3. Start the app and verify the dashboard loads — it should say "Sneebly" everywhere, not "AppPilot"

Tell me when this phase is complete and show me the grep results confirming no AppPilot references remain.
