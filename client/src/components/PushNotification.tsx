// Removed: in-app notification popup replaced by native push notifications via service worker.
// Real clock-in reminders are delivered as native push notifications from the geofencing backend.
export default function PushNotification() {
  return null;
}
